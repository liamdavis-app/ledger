.DS_Store

# Local-only: business planning, not for the public portfolio repo
PAID-APP-PLAN.md
LOCAL.md

# Local-only: decision log. This repo is public, and the log records internal reasoning
# plus an unresolved note about real training data being present here — publishing it
# would point at exactly what it describes.
MEMORY.md

# Local tooling config
.claude/

# Local-only: the real training sessions removed from index.html on 2026-08-16.
# Kept out of this public repo deliberately. Also recoverable from git history.
BACKFILL-real.local.js
